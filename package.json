{
  "name": "task-manager",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend start\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}